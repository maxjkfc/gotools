# addItemTocsv


## Build
```
    go build  .
```

## Use
```
    addItemTocsv -f { inputfile}
                 -o { output file}
                 -s { want to insert string }
                 -r { want to insert rows }
                 -a { action (insert / remove) }
```

